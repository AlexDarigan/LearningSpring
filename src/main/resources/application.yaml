logging:
  level:
    org:
      springframework:
        jdbc:
          datasource:
            init:
              ScriptUtils: debug

spring.jpa:
    hibernate:
      ddl-auto: none

spring:
  jpa:
    hibernate:
      use-new-id-generator-mappings: false
